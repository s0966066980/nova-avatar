<!--
Copyright (c) 2026 HongXian0903
SPDX-License-Identifier: Apache-2.0
-->

# Nova Avatar

即時數字人對話系統的共通語言，涵蓋使用者語音、數字人回覆與雙方輪替規則。

## Language

**免按對話模式**：
WebRTC 連線成功後，在數字人未說話時立即等待並擷取使用者語音的互動模式。使用哪一種 VAD 引擎不是此模式的定義。
_Avoid_: Silero 模式、VAD 模式、連續錄音模式

**插話**：
數字人正在回覆時，使用者明確按下麥克風按鈕，先結束該次回覆，再開始新的使用者發話；不包含喇叭迴音或背景人聲造成的誤觸發。
_Avoid_: 迴音觸發、自動打斷

**收音暫停**：
保留已授權的麥克風串流，但不錄製、不切分使用者發話，也不送入語音辨識的狀態。
_Avoid_: 背景錄音、錄完丟棄、關閉麥克風

**對話輪次**：
從系統開始等待一段使用者發話，到該發話的數字人回覆播放完並結束尾音保護為止；同一時間只允許一個輪次進行。
_Avoid_: 重疊請求、並行發話

**發話狀態事件**：
數字人開始或停止輸出回覆語音時，由伺服器主動送出的會話事件，是收音暫停與恢復的依據。
_Avoid_: 前端音量猜測、發話狀態輪詢

**使用者發話串流**：
免按對話可收音期間，由瀏覽器透過雙向 WebRTC 音軌持續送往伺服器、交由 Silero 判定發話邊界的麥克風音訊。
_Avoid_: 前端先切段的錄音檔、RMS 切段

**輪次編排**：
一段使用者發話完成後，依序產生辨識文字、模型回覆與數字人語音的完整處理責任；由伺服器單獨擁有。
_Avoid_: 前後端分段接力、前端重送錄音

**語音轉文字（STT）**：
將 Silero 切出的完整使用者發話轉成文字的處理步驟；實作與程式介面可沿用 ASR 名稱。
_Avoid_: 語音理解、VAD

**回覆語音串流**：
數位人在同一對話輪次的模型回覆尚未完整生成時，持續將已確認的文字片段交付後續語音與播放流程；不包含使用者尚未說完就提前回覆。它與舊有回覆模式的差異僅在回覆生成與交付粒度，不改變字幕、播放與錯誤恢復保證。
_Avoid_: Voice Streaming、全雙工、等待完整回答後朗讀

**舊有回覆模式**：
模型先產生完整回覆，再將完整回覆交付後續語音與播放流程的回覆模式。它不是相容或降級模式，與回覆語音串流共用相同的字幕、播放與錯誤恢復保證。
_Avoid_: 相容模式、降級模式、缺少串流保證的模式

**輪次隔離**：
只有目前有效對話輪次的文字、語音與數位人影格可以送出；輪次取消一經確認，尚未播放的舊輪次資料必須在每個輸出邊界被丟棄。
_Avoid_: 只清佇列、盡力取消、允許舊回覆收尾

**回覆背壓**：
待合成或待播放內容達到延遲預算時，先讓模型回覆串流等待；若持續滿載，才在完整語意邊界終止剩餘回覆。
_Avoid_: 無限排隊、靜默丟句、固定字數硬切

**可播回覆片段**：
模型回覆中已形成完整短語意、可以獨立交給語音合成與播放的最小單位。
_Avoid_: LLM token、固定字數塊、不完整詞句

**舞台字幕顯示窗口**：
數字人舞台在單一對話輪次中呈現的最新回覆文字範圍；它是即時觀看用的有限窗口，不是完整回覆或對話歷史。
_Avoid_: 完整回覆、對話歷史、舞台保留全文

**舞台字幕顯示上限**：
由控制台設定、限制舞台字幕顯示窗口大小的字數；它以使用者感知的字元計算，超限時淘汰最早的完整可播回覆片段，不截斷片段。
_Avoid_: 回覆生成上限、固定字數切片、硬切字幕

**首音延遲**：
從系統確認使用者發話結束，到送出該輪次第一個非靜音 WebRTC 回覆音訊幀的時間。
_Avoid_: LLM 首 token、TTS 首 byte、播放器開始事件

**已播回覆**：
數位人已實際送出回覆音訊、因而可視為使用者已收到的內容；僅由模型生成或仍在佇列中的文字不算已播回覆。
_Avoid_: 完整模型回答、待播文字、已合成但未送出的語音

**回覆預覽**：
控制台在同一對話輪次仍在進行時顯示的模型文字；它不是已播回覆。輪次結束後，控制台紀錄必須改為已播回覆，未出聲的預覽不得留下。
_Avoid_: 完整回覆、對話歷史、舞台字幕、已播回覆

**輪次提交**：
對話輪次結束時，將使用者發話文字與該輪次的已播回覆寫入對話歷史，並作為控制台靜態紀錄的唯一依據；取消或截斷不得提交未播放的模型內容。
_Avoid_: 提交完整生成內容、取消即整輪遺忘、以模型寫完當作提交

**音訊主時鐘**：
以回覆音訊時間軸作為數位人播放進度的權威；嘴型推理落後時可以丟幀或短暫重複，但不得讓影格排隊拖慢語音。
_Avoid_: 視訊主時鐘、為保留影格延遲語音、無界媒體排隊

**嘴型連續控制**：
在 MuseTalk 說話影格與原始 idle 影格交界，只對嘴部遮罩範圍做固定影格數的視覺過渡；它不等待音訊、不執行額外即時推理，也不改變媒體時鐘。
_Avoid_: 全畫面淡入淡出、以音訊緩衝換嘴型平滑、長時間凍結張嘴影格

**待機回復過渡**：
數位人送出一輪回覆的最後說話影格後，視覺回到自然待機動作的有限影格區間；其平滑處理不得延後回覆音訊或改變音訊主時鐘。
_Avoid_: 回答完成、停止串流、全畫面淡出

**回答收尾穩定**：
待機回復過渡中，以持續移動的待機影格作為視覺目標，逐步解除最後說話嘴型的短暫狀態；它只處理嘴部區域，不延長回答語音。
_Avoid_: 凍結最後影格、生成接續語音、全畫面轉場

**待機回復抖動**：
待機回復過渡中，嘴部、臉部或角色位置出現不符合待機動作的瞬間往返、錯位或鬼影；以過渡區域與區域外的相鄰影格差異配合真實 WebRTC 多輪播放判定。
_Avoid_: 單指嘴型閃跳、一般待機動作、只靠主觀感受

**Owned llama-server**：
由本次 Nova Avatar backend 程序透過 `Popen` 啟動並持有 process handle 的 llama.cpp 服務；只有這類程序會在 backend 正常關閉時自動停止。
_Avoid_: 依 port 猜測所有權、關閉外部管理的 llama-server、把 SIGKILL 當成可清理關閉

**看板回覆**：
同一回答中供使用者以條列方式閱讀的具體項目，與數字人口述內容分開呈現；只有需要分項閱讀的回答才使用。
_Avoid_: 任務看板、舞台字幕、所有回覆自動條列

**口語摘要**：
搭配看板回覆時，由數字人口述的主要結論與概略說明，具體項目由看板補充。
_Avoid_: 逐項朗讀看板、完整回答全文

**已顯示看板項目**：
在該場對話的看板中已呈現的完整條列項目，可作為後續追問的依據；它不代表使用者已閱讀，也不包含尚未顯示的生成內容。
_Avoid_: 已播回覆、已讀項目、所有生成項目

**回覆規則（Rule）**：
指導同一個 LLM 決定何時直接口述、何時使用看板，以及口語摘要與看板項目如何呈現的可修改指令。
_Avoid_: 關鍵字計分表、獨立分類模型、看板外觀設定

**規則套用**：
將編輯中的回覆規則確立為後續對話輪次共用的規則；已開始的輪次仍沿用開始時的規則。
_Avoid_: 輸入即生效、中途替換規則、只儲存草稿
